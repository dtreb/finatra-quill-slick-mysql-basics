## Customize database setup
database:
  override:
    - mysql -u ubuntu -e "CREATE USER 'root'@'localhost' IDENTIFIED BY 'den'"
    - mysql -u ubuntu -e "GRANT ALL PRIVILEGES ON * . * TO 'root'@'localhost'"
    - mysql -u root -pden -e "SOURCE sql/init.sql"